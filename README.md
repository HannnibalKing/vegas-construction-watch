# Vegas Construction Watch

A fixture-first API for discovering approved and upcoming construction/development projects around Las Vegas.

```bash
npm test
npm start
```

`GET /api/projects` returns normalized project locations, status, dates, and source metadata. Add adapters for City of Las Vegas open data, Clark County development services, permit records, and approved-planning feeds.