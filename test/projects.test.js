const test=require('node:test');const assert=require('node:assert/strict');const {upcoming}=require('../server');test('returns approved future projects ordered by date',()=>{const r=upcoming([{id:'late',status:'approved',expectedStart:'2027-01-01'},{id:'soon',status:'approved',expectedStart:'2026-09-01'},{id:'denied',status:'denied',expectedStart:'2026-08-01'}],new Date('2026-08-01'));assert.deepEqual(r.map(x=>x.id),['soon','late']);});
